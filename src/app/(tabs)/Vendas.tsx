import React = require('react');
import { Text, View } from '@/src/components/Themed';

export default function Vendas() {
  return (
    <View className='flex flex-1 items-center justify-center'>
      <Text className='font-bold text-2xl'>Vendas</Text>
    </View>
  );
}
